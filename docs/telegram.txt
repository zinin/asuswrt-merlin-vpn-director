изучи документацию и кодовую базу, сейчас процесс выглядит следующим образом:
1) установка через install.sh
2) скачивание серверов через import_server_list.sh
3) конфигарация через configure.sh

1-й этап в любом случае остаётся

но я хочу написать ещё приложение на Go(которое будет компилироваться под архитектуру роутера), которое будет работать работать через telegram бота.

пользователь открывает бота и ему доступен функционал
1) обновить сервера, аналогичен import_server_list.sh, пользователь вводит ссылку - сервера обновляются(но не через скрипт, а нативный код на Go, но полностью идентичный по смыслу)
2) конфигурация, аналогичен configure.sh только вместо терминала обмен сообщениями с ботом

нужно продумать:
1) как передать bot-token для работы с ботом в приложение
2) каким образом авторизовать пользователя в телеграм боте, то есть кто может пользоваться ботом

я думаю, сейчас можно добавить ещё один скрипт, который пользователь может запустить после install.sh, например, setup_telegram_bot.sh
то есть по сути бот - опциональный функционал, можно работать, как с ним, так и из консоли, оба способа могут работать одновременно, они делают одно и тоже, просто разный интерфейс

и там спросят token и username пользователя, который имеет право управлять ботом
для бота нужно сделать отдельный json конфиг рядом типа telegram-bot.json

в нём хранить все настройки бота, token и username, а потом ещё id чатов, если потребуется(они появятся только после того, как пользователь-администратор напишет боту)

давай сделаем brainstorm на эту тему, задавай мне любые вопросы, я буду отвечать
на выходе хочу увидеть дизайн постановку, как будто ты бизнес и системный аналитик

