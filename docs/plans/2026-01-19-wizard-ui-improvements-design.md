# Wizard UI Improvements Design

## Overview

Улучшение UI wizard в Telegram боте для консистентности с консольным вариантом `configure.sh`.

## Проблемы

1. **Step 2 (Exclusions):** только 4 страны (ru, ua, by, kz), в консоли — 10
2. **Step 2:** галочка `v` неочевидна
3. **Step 1 (Servers):** нет нумерации серверов, в консоли есть

## Решения

### Step 1: Server Selection

**Нумерация:**
- Добавить номер в начале: `1. 🇧🇷 Бразилия, Сан-Паулу`

**Динамическая сетка:**
- ≤5 серверов → 1 колонка
- 6-10 серверов → 2 колонки
- >10 серверов → 3 колонки

**Обрезка названий:**
- При 3 колонках обрезать длинные названия для помещения в кнопку

### Step 2: Exclusions

**Расширенный список стран (10 вместо 4):**
```
ru - Russia       fr - France
ua - Ukraine      nl - Netherlands
by - Belarus      pl - Poland
kz - Kazakhstan   tr - Turkey
de - Germany      il - Israel
```

**Формат кнопок:**
- Выбрано: `✅ ru Russia`
- Не выбрано: `🔲 ua Ukraine`

**Расположение:**
- Сетка 5x2 (5 строк по 2 кнопки)

## Изменения в коде

### telegram-bot/internal/wizard/wizard.go

Расширить `DefaultExclusions`:
```go
var DefaultExclusions = []string{
    "ru", "ua", "by", "kz", "de",
    "fr", "nl", "pl", "tr", "il",
}
```

Добавить map с названиями:
```go
var CountryNames = map[string]string{
    "ru": "Russia", "ua": "Ukraine", "by": "Belarus",
    "kz": "Kazakhstan", "de": "Germany", "fr": "France",
    "nl": "Netherlands", "pl": "Poland", "tr": "Turkey",
    "il": "Israel",
}
```

### telegram-bot/internal/bot/wizard_handlers.go

**sendServerSelection:**
1. Вычислять количество колонок по длине списка
2. Добавить нумерацию: `fmt.Sprintf("%d. %s", i+1, s.Name)`
3. Группировать кнопки в строки по N колонок
4. Обрезать длинные названия при 3 колонках

**sendExclusionsSelection:**
1. Формировать кнопки по 2 в ряд вместо 4
2. Использовать `✅`/`🔲` вместо `v `/`  `
3. Добавлять название страны из `CountryNames`

## Пример UI

**Step 1 (57 серверов → 3 колонки):**
```
[1. 🇧🇷 Бразилия] [2. 🇫🇮 Финляндия] [3. 🇲🇽 Мексика]
[4. 🇵🇱 Польша]   [5. 🇩🇪 Германия]  [6. 🇳🇱 Нидерланды]
...
```

**Step 2 (сетка 5x2):**
```
[✅ ru Russia]    [🔲 fr France]
[🔲 ua Ukraine]   [🔲 nl Netherlands]
[🔲 by Belarus]   [🔲 pl Poland]
[🔲 kz Kazakhstan][🔲 tr Turkey]
[🔲 de Germany]   [🔲 il Israel]
[Done]            [Cancel]
```
